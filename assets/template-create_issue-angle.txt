🎯 **Goal**
Replace the character used for angle brackets in the {language} translation of `{file}` to display the error code correctly.

🐞 **Problem**
The characters `<` and `>` was used in the collapsible section (**`{summary}`**) of the [{chapter_name}]({chapter_link}) chapter, causing `<your-username>` to be treated as code and therefore not visible.

💡 **What needs to be done**
- Locate the line begin with: `fatal: Authentication failed ...`
- Replace `<` with `&lt;` in this line
- Replace `>` with `&gt;` in this line

🔗 **Link to the file**
{filepath}

🗺️ **Example of what needs to be fixed**
- Before the replacement:
`fatal: Authentication failed for 'https://github.com/<your-username>/first-contributions.git/'`
- After the replacement:
`fatal: Authentication failed for 'https://github.com/&lt;your-username&gt;/first-contributions.git/'`

> [!Important]
> Please only address one issue in a PR.
> Let's make it possible for more people to solve issues here.

📋 **Steps to solve the problem**
1. Find the line starting with: `fatal: Authentication failed ...`.
2. Replace the angle brackets.
3. Add, commit, push your changes.
4. Submit a pull request and **add this in comments: `Resolves #xxxxxx`**, where #xxxxxx is the issue number.
5. Return to this issue and **leave a comment below with your PR number (example: #000000) or link** to ask for a review.
6. Celebrate your contribution to this project. 🎉